This header is included by NSS consumers which may be built as C89, where
gcc does not recognise "inline" as a keyword. In particular, illumos builds
with -fno-asm which removes it in gnu89 mode. Use the __inline__ form, which
is always available.

diff -wpruN --no-dereference '--exclude=*.orig' a~/lib/util/eccutil.h a/lib/util/eccutil.h
--- a~/lib/util/eccutil.h	1970-01-01 00:00:00
+++ a/lib/util/eccutil.h	1970-01-01 00:00:00
@@ -41,7 +41,7 @@ typedef enum {
  * known: the point is then reported as not bare, so a caller deciding whether
  * to unwrap falls back on trying the decode.
  */
-static inline PRBool
+static __inline__ PRBool
 ECPoint_IsBare(const SECItem *point, unsigned int fieldLen)
 {
     if (fieldLen == 0 || point == NULL || point->data == NULL) {
