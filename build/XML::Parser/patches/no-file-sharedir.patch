diff -ur a/Expat/Expat.pm b/Expat/Expat.pm
--- a/Expat/Expat.pm	2026-08-22 00:57:56
+++ b/Expat/Expat.pm	2026-08-22 00:58:51
@@ -13,15 +13,10 @@
 our ( %Encoding_Table, @Encoding_Path );
 
 use File::Spec ();
-use File::ShareDir ();
 
 %Encoding_Table = ();
 
-my $_share_dir;
-eval { $_share_dir = File::ShareDir::dist_dir('XML-Parser') };
-
 @Encoding_Path = (
-    ( defined $_share_dir && -d $_share_dir ? ($_share_dir) : () ),
     grep( -d $_,
         map( File::Spec->catdir( $_, qw(XML Parser Encodings) ), @INC ) ),
     File::Spec->curdir
diff -ur a/Makefile.PL b/Makefile.PL
--- a/Makefile.PL	2026-08-22 00:57:56
+++ b/Makefile.PL	2026-08-22 00:58:59
@@ -4,8 +4,6 @@
 use ExtUtils::MakeMaker;
 use lib './inc';
 use Devel::CheckLib;
-use File::ShareDir::Install;
-install_share dist => 'share';
 
 use Config;
 use File::Spec;
@@ -154,12 +152,6 @@
     $ret;
 }
 
-sub MY::postamble {
-    my $self = shift;
-    my @ret  = File::ShareDir::Install::postamble($self);
-    return join "\n", @ret;
-}
-
 my $dist_version = MM->parse_version('Parser.pm');
 
 WriteMakefile1(
@@ -224,10 +216,6 @@
     VERSION_FROM => 'Parser.pm',
     CONFIGURE_REQUIRES => {
         'ExtUtils::MakeMaker'     => 0,
-        'File::ShareDir::Install' => '0.06',
-    },
-    PREREQ_PM => {
-        'File::ShareDir' => 0,
     },
     $^O =~ /win/i
     ? (
